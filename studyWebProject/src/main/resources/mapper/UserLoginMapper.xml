<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.myproject.mapper.usermenu.UserLoginMapper">

<!-- 로그인 처리 -->
<select id="selectUserAccount" resultType="UserDTO">
SELECT * FROM user_master WHERE user_id = #{id} AND user_pw = #{pw}
</select>

</mapper>